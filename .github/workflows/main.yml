name: sunBlog deploy github action

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
      - run: yarn install
      - run: yarn deploy
